<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css"
      />
    <title>User</title>
  </head>
  <body class="container">
    <div class="alert alert-success">
      <!-- based on the structure of your messages, determine how to display them -->
      <% if(messages.submitting) { %> <% for (var x of messages.submitting)
      { %>
      <h3><%= x %></h3>
      <% } %> <% } %>
    </div>

    <form action="/quotes" method="POST">
      <div class="form-group">
        <label for="name">Name:</label>
        <input type="text" name="name" class="form-control" />
      </div>
      <div class="form-group">
        <label for="quote">Quote:</label>
        <textarea class="form-control" rows="5" name="quote" id="quote"></textarea>
      </div>
      <button type="submit" class="btn btn-primary btn-lg active">Submit</button>
      <a href="http://localhost:8000/quotes" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Skip to quotes!</a>
    </form>
  </body>
</html>
